{
  "name": "fun-backgrounds",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Background images, kept as quality-80 JPEGs converted from the lossless PNG masters in RAW/ (masters stay local)",
  "scripts": {
    "convert": "node scripts/convert-raw.mjs",
    "gallery": "node scripts/build-gallery.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
